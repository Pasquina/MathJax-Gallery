## Why it belongs
Often cited as the most beautiful equation in mathematics.

### Code
```text
$$
\frac{d}{dx} \left( \int_a^x f(t)\,dt \right) = f(x)
$$
```

### Renders As
$$
\frac{d}{dx} \left( \int_a^x f(t)\,dt \right) = f(x)
$$

# Why it belongs
This theorem is calculus.